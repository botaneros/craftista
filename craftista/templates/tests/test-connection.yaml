apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "craftista.fullname" . }}-test-connection"
  labels:
    {{- include "craftista.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "craftista.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
